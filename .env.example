# ExoView environment variables (example)
# Copy to .env and fill in as needed. No secrets are required for the MVP.

# Base path when deploying under a sub-path (e.g. GitHub Pages). Default "/".
VITE_BASE_PATH=/
